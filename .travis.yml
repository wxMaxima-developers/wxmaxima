language: cpp
sudo: required

branches:
  only:
    - master

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libwxgtk3.0-dev

script: |
  ./bootstrap &&
  ./configure &&
  make &&
  make dist
