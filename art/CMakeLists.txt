add_subdirectory(config)
add_subdirectory(statusbar)
# For Windows we need install these icons - for Unix/Linux they are usually
# installed with GTK. But not always, it seems.
add_subdirectory(toolbar)
