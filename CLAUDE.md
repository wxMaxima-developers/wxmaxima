# Project instructions for Claude Code

The instructions live in [AGENTS.md](AGENTS.md), so that every agent -- and every
human contributor -- reads the same file instead of a per-tool copy that drifts
out of step. This file only imports it:

@AGENTS.md

Personal notes and machine-specific setup do not belong here: put them in
`CLAUDE.local.md`, which is git-ignored.
